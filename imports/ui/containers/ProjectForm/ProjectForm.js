import React from 'react';
import ProjectFormLayout from '../../components/ProjectFormLayout/'


const ProjectForm = ({ handleSubmit }) => (
    <div>
		<ProjectFormLayout handleSubmit={handleSubmit} />
	</div>
);

export default ProjectForm;
