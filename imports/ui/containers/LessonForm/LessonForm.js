import React from 'react';
import LessonFormLayout from '../../components/LessonFormLayout/'


const LessonForm = ({ handleSubmit }) => (
    <div>
		<LessonFormLayout handleSubmit={handleSubmit} />
	</div>
);

export default LessonForm;
